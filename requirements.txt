#epics-containers-cli==2.1.0
# use the branch dev in requirements.txt when debugging CI i.e. :
epics-containers-cli -e git+https://github.com/epics-containers/epics-containers-cli.git@dev#egg=epics-containers-cli
